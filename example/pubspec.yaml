name: example
description: An example of how to use flutter_app_wrapper
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_app_wrapper:
    path: ..

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true